template(name='exercice')
  .container-exerice.container
    if this.exercice
      //- p
      //-   |{{this.exercice.question}}
      form
        input(type='hidden', name='exercice_id', value='{{this._id}}')
        if isTeacher currentUser
          +exerciceViewTeacher
        else
          +exerciceViewStudent
